---
export interface Props {
    title: string;
}

const { title } = Astro.props;
---

<ul role="list" class="flex flex-col space-y-4 bg-neutral-50 rounded p-4 shadow-md h-min">
    <h2 class="font-bold text-xl text-center">{title}</h2>
    <slot />
</ul>